<svg viewBox="0 0 800 1000" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <pattern id="grid" width="40" height="40" patternUnits="userSpaceOnUse">
      <path d="M 40 0 L 0 0 0 40" fill="none" stroke="rgba(255,255,255,0.1)" stroke-width="1"/>
    </pattern>
    
    <mask id="s-mask">
      <path 
        d="M600,750 
        C600,850 500,900 350,900 
        C200,900 100,850 90,750 
        L200,750 
        C210,800 250,825 350,825 
        C450,825 475,800 475,750 
        C475,650 90,700 90,450 
        C90,350 190,300 340,300 
        C490,300 590,350 600,450 
        L490,450 
        C480,400 440,375 340,375 
        C240,375 215,400 215,450 
        C215,550 600,500 600,750 Z" 
        fill="white"
      />
    </mask>
  </defs>

  <!-- Main S shape with warehouse visual elements -->
  <g mask="url(#s-mask)">
    <!-- Background grid -->
    <rect width="800" height="1000" fill="url(#grid)"/>
    
    <!-- Warehouse shelves -->
    <g stroke="rgba(255,255,255,0.2)" stroke-width="2" fill="none">
      {Array.from({length: 10}).map((_, i) => (
        <rect x="100" y={100 + i * 80} width="600" height="60"/>
      ))}
    </g>

    <!-- Boxes and containers -->
    <g fill="rgba(255,255,255,0.15)">
      {Array.from({length: 15}).map((_, i) => (
        <rect 
          x={100 + (i % 5) * 120} 
          y={120 + Math.floor(i / 5) * 80} 
          width="100" 
          height="40"
        />
      ))}
    </g>

    <!-- Forklift silhouettes -->
    <path 
      d="M200,400 h40 v-30 h60 v30 h20 v20 h-120 v-20 
         M240,420 a10,10 0 1,0 20,0 a10,10 0 1,0 -20,0
         M280,420 a10,10 0 1,0 20,0 a10,10 0 1,0 -20,0"
      fill="rgba(255,255,255,0.2)"
    />
    
    <path 
      d="M500,700 h40 v-30 h60 v30 h20 v20 h-120 v-20 
         M540,720 a10,10 0 1,0 20,0 a10,10 0 1,0 -20,0
         M580,720 a10,10 0 1,0 20,0 a10,10 0 1,0 -20,0"
      fill="rgba(255,255,255,0.2)"
    />

    <!-- Conveyor belt elements -->
    <g stroke="rgba(255,255,255,0.15)" stroke-width="2">
      {Array.from({length: 20}).map((_, i) => (
        <line 
          x1={200} y1={200 + i * 30} 
          x2={600} y2={200 + i * 30}
        />
      ))}
    </g>

    <!-- Logistics flow lines -->
    <path 
      d="M150,300 C250,350 350,300 450,350 S650,300 750,350
         M150,500 C250,550 350,500 450,550 S650,500 750,550
         M150,700 C250,750 350,700 450,750 S650,700 750,750"
      stroke="rgba(255,255,255,0.1)" 
      stroke-width="3"
      fill="none"
    />
  </g>

  <!-- Overlay effects -->
  <g mask="url(#s-mask)">
    <rect width="800" height="1000" fill="url(#gradient)"/>
    <animateTransform 
      attributeName="transform"
      type="translate"
      from="-800 0"
      to="800 0"
      dur="20s"
      repeatCount="indefinite"
    />
  </g>
</svg>